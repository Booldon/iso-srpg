# Godot-specific
.godot/
*.uid

# Export presets (may contain sensitive paths)
export_presets.cfg

# Runtime saves
saves/

# OS
.DS_Store
Thumbs.db
*:Zone.Identifier

# Claude Code — machine-specific permissions (do not commit)
.claude/settings.local.json
.claude/scripts/.last_summary_ts
