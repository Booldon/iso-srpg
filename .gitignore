# Godot-specific
.godot/
*.uid

# Export presets (may contain sensitive paths)
export_presets.cfg

# Runtime saves
saves/

# OS
.DS_Store
Thumbs.db

# Claude Code — machine-specific permissions (do not commit)
.claude/settings.local.json
